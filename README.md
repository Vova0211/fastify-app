# Simple backend-server on js/fastify

## How start server
Generate a jwt secret key

### Create .env file:
```env
SUPABASE_URL=
SUPABASE_ANON_KEY=
JWT_SECRET=write down the generated key here
```
<!-- 
```env
DB_HOST= 
DB_NAME=
DB_PORT=
DB_USER=
DB_PASSWORD=
JWT_SECRET=write down the generated key here
``` -->
### Write on terminal `make start`